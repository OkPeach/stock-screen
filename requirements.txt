# Pipeline runtime (runs in GitHub Actions only). Python 3.11+.
# Technicals (SMA/RSI/MACD) are computed directly with pandas — pandas-ta is
# unavailable for 3.11 here and its 0.3.x line breaks under NumPy 2.x.
requests>=2.31
pandas>=2.1
pyyaml>=6.0
